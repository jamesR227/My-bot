module.exports = {
    kickUser: function (message, args) {
        // Check permissions, mention the user to kick, etc.
        // Use message.guild.member(user).kick() to kick the user
    },
    banUser: function (message, args) {
        // Check permissions, mention the user to ban, etc.
        // Use message.guild.member(user).ban() to ban the user
    }
    // Add more moderation commands as needed
};
